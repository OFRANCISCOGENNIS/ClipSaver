apiVersion: v1
kind: Service
metadata:
  name: vidora-api
  labels:
    app.kubernetes.io/name: vidora-api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vidora-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
