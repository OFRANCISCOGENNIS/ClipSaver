# Dart / Flutter
app/.dart_tool/
app/build/

# Node
node_modules/
server/node_modules/
server/dist/
server/coverage/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Flutter tool state (machine-specific, regenerated by `flutter pub get`)
app/.flutter-plugins-dependencies
app/.dart_tool/

# app/pubspec.lock IS committed: app/ is an application, so builds
# must be reproducible from the locked dependency set.
